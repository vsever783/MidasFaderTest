<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:label="Midas"
        android:theme="@style/Theme.AppCompat.DayNight">

        <!-- ОДНА точка входа - экран выбора роли (звукорежиссёр/музыкант).
             Раньше было ДВА отдельных значка с разными taskAffinity, что
             позволяло случайно запустить оба режима ОДНОВРЕМЕННО с одного
             и того же телефона (один IP) - а пульт различает клиентов
             только по IP, не по порту, и путался, куда слать ответы
             (подтверждено реальным захватом трафика). Теперь оба режима
             используют общее подключение (порт 10001) внутри MainActivity,
             так что такая путаница в принципе невозможна. -->
        <activity
            android:name=".RoleSelectActivity"
            android:label="Midas"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="Midas"
            android:exported="false" />

    </application>
</manifest>
