# Сессия на пульте — порядок действий

Пункты идут от самых дешёвых и важных к длинным. Если времени мало,
делай 1–4: они закрывают вопросы, которые блокируют доверие ко всему
остальному.

**Перед началом:** запусти Wireshark на весь заход одним файлом и веди
текстовую заметку с временем каждого действия — потом сопоставим.

---

## 1. Регрессия после правок интерфейса (2 минуты)

Проверяем, что ничего не развалилось.

1. Подключиться → открыть канал → пройти по всем шести вкладкам:
   `INPUT` `ROUTING` `COMP` `SENDS` `EQ` `GATE`.
2. Убедиться, что на планшете в **альбомной** ориентации содержимое
   `ROUTING` помещается **без вертикальной прокрутки**.
3. Выйти в выбор режима → вернуться → снова открыть `MATRIX` и `AUX`.
   Значения должны быть живые, а не застывшие.
4. Проверить, что по краям экрана нет белых полос.

---

## 2. Toggle-семантика (5 минут) — ВАЖНО

**Зачем:** код в одних местах шлёт `1/0`, в других константу `1`.
Датасет описывает mute/solo/phantom как toggle. Одно из двух неверно.

**Тест для каждого:** `enMuteStatus`, `enFaderSolo`,
`enMicSplitPhantomPowerIn`, `enInputPhaseIn`, `enInputHighPassFltIn`,
`enExpGateIn`, `enCompLimFiltersIn`.

1. Включить из приложения → **выключить из приложения**.
2. Включить с пульта → выключить из приложения.

**Что записать:** срабатывает ли выключение. Если хотя бы где-то нет —
параметр toggle, и все `if (on) 1 else 0` надо менять на константу `1`.

---

## 3. Мьют-группы (5 минут)

Структура взята из датасета, своим захватом **не подтверждена**.

1. Открыть вкладку `MUTE` — должно быть ровно **6** полос (не 8).
2. Замьютить группу **с пульта** → кнопка в приложении должна
   подсветиться красным.
3. Замьютить группу **из приложения** → должно сработать на пульте.
4. Открыть канал → вкладка `ROUTING` → строка `MUTE GROUPS`:
   нажать группу 1 → проверить на пульте, что канал в неё попал.
5. Сверить: назначить канал в две группы сразу — он должен молчать,
   пока замьючена хотя бы одна.

---

## 4. Посылы в матрицу (5 минут)

Добавлены недавно, на железе не проверялись ни разу.

1. Канал → `SENDS` → пролистать вправо до синих `MTX 1–8`.
2. Подвигать `MTX 1` → проверить на пульте, что меняется посыл
   в матрицу 1 (а **не** в мастер и не в аукс).
3. Нажать `ON/OFF` и `PRE/POST` на `MTX 1` → сверить с пультом.

Если посыл уходит не туда — запиши, куда именно; значит имя
`enMainSendLevel` относится к другой шине.

---

## 5. Кнопка TAP (3 минуты)

Сейчас не работает. Адрес `enGlobalTapSwitch` подтверждён символами в
прошивке, но у него `multiPath: false` — есть подозрение, что путь не
должен содержать сегмент `/enGlobals/`.

1. Открыть на пульте страницу, где виден темп делея.
2. Нажать `TAP` в приложении несколько раз в ритме.
3. **Отдельно**: постучать `TAP` на самом пульте — это главное, нужен
   захват того, что шлёт сам пульт.

---

## 6. Кнопки SLOPE (3 минуты)

Новые, состояние с пульта не читается — только переключают.

1. Канал → `ROUTING` → `HP SLOPE` нажать несколько раз.
2. Смотреть на пульте, меняется ли крутизна (12 / 24 дБ на октаву).
3. То же с `LP SLOPE` (6 / 12 дБ на октаву).

---

## 7. LINK (3 минуты)

`enConfigPairingState` возвращает живые значения, но наши записи
игнорирует — 4 записи подряд не изменили ничего.

1. Нажать `LINK` на паре каналов из приложения.
2. Проверить на пульте.
3. Затем **связать пару с самого пульта** — нужен захват того, что при
   этом уходит по сети.

---

## 8. Калибровка гейта (10 минут)

Диапазоны взяты из мануала, но форма кривой между крайними точками —
предположение.

Для каждой ручки: вывернуть **в минимум**, записать что показывает
пульт; вывернуть **в максимум**, записать; поставить **в середину
хода**, записать.

- Threshold (ожидаем −50 … +25 dBu)
- Range (−100 … 0 дБ)
- Attack (20 мкс … 20 мс)
- Hold (5 мс … 2 с)
- Release (2 мс … 2 с)
- Filter Freq (50 Гц … 15 кГц)
- **Transient** — диапазона нет в мануале вообще, тут особенно нужны
  крайние значения

Середина хода важнее краёв: края мы знаем, а форму кривой — нет.

---

## 9. Если останется время: GAIN и Comp Ratio

1. **GAIN**: вывернуть в минимум и максимум. Ожидаем −5 … +40 дБ
   **шагами по 5**. Если шаг 2.5 и диапазон −2.5 … +45 — значит в
   тракте DL431 Mic Splitter, и нужна вторая шкала.
2. **TRIM**: ожидаем −40 … +20 дБ непрерывно.
3. **Comp Ratio**: максимум должен быть **25:1** (я исправлял с 10:1).

---

## Что прислать после сессии

- Файл `.pcapng` целиком.
- Текстовую заметку: время + что делал.
- Фото экрана пульта для гейта (пункт 8) — числа со шкал.
