<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#1c1c1e"
        android:padding="12dp"
        android:gravity="center_vertical">

        <Button
            style="@style/Widget.Midas.Button"
            android:id="@+id/btnMuteGroupMembersBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minHeight="0dp"
            android:text="← Back"
            android:textColor="#ffffff"
            android:backgroundTint="#3a3a3c" />

        <TextView
            android:id="@+id/textMuteGroupMembersTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:text="MUTE GROUP 1 MEMBERS"
            android:textColor="#ff9f0a"
            android:textStyle="bold"
            android:textSize="18sp" />
    </LinearLayout>

    <!-- НЕ подтверждено реальным захватом трафика - структура взята из
         стороннего датасета (см. заметку в Pro2Commands.kt у vcaChild*Address). -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:text="⚠ Не подтверждено реальным захватом - проверяйте внимательно"
        android:textColor="#8e8e93"
        android:textSize="11sp"
        android:gravity="center" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:id="@+id/containerMuteGroupMembers"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp" />
    </ScrollView>

</LinearLayout>
