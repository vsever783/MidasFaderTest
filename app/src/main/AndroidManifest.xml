<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:label="Midas"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Midas">

        <!-- ОДНА цепочка входа: подключение -> выбор роли -> сам режим.
             Раньше подключение было встроено прямо в MainActivity и
             целиком пересоздавалось при каждом переключении режима - это
             было тяжело для пульта. Теперь ConnectActivity подключается
             ОДИН раз, дальше RoleSelectActivity выбирает режим, а
             MainActivity переиспользует уже живое соединение (см.
             MainActivity.beginModeSession()). -->
        <activity
            android:name=".ConnectActivity"
            android:label="Midas"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RoleSelectActivity"
            android:label="Midas"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:label="Midas"
            android:exported="false" />

    </application>
</manifest>
